# 项目决策

## 2026-08-26 — Benchmark adapter 通用化与 FEV-Bench Mini 外部验证

CaFE 新增 benchmark-neutral 的原生预测窗口契约。Adapter 只统一 target、
动态/静态协变量、mask、任务身份、frequency、seasonality、context 和 horizon
的表示，不重新定义外部 benchmark 的样本切分。GIFT-Eval 继续使用其官方
term、rolling-origin 与完整原生历史；FEV-Bench 使用固定版本的官方
`tasks_mini.yaml`、`Task.iter_windows()`、cutoff、min/max context、horizon 和
covariate visibility。Treatment 作用于 benchmark 原生 context，模型最大
context 截断仍在 inference 后置执行。源数据保持单份快照，generation 仅保存
compact locator 与 treatment contract，不物化重复的稠密标准样本。

实现先抽取通用 `NativeForecastInstance` 和 adapter 接口，并要求 GIFT 的实例、
契约与既有协议无回归；随后接入 FEV Mini 20-task。模型资格不再由通用核心中的
GIFT term 决定，而按 task 的实际 horizon、模型输出限制和本次实验明确选择的
模型上下文判断。FEV 的 static/categorical/binary covariate 保留原生语义；
`covariate_impulse_response` 第一版只允许满足连续数值资格的动态协变量。

GIFT 与 FEV 都在 analysis 阶段执行 dataset/task 等权的 suite 聚合，不新增第五
阶段；逐 instance 结果先在 task 内汇总，再进行等权汇总和 paired task bootstrap，
避免大数据集支配结论。FEV 第一版以 point-forecast MASE 和 CaFE paired effect
为正式结果，不宣称完成 SQL probabilistic leaderboard。日常及 smoke 流水线只
要求 research validation；publication validation 保留为发布冻结时的显式模式。
新协议使用新的 pipeline/adapter schema 和 experiment id，既有实验产物不改写。

## 2026-08-24 — 默认流水线关闭状态依赖持续性

默认 capability 集合不再包含 `nonlinear_persistence`。该机制在最近完整实验中
的最佳 effect NRMSE 在 short、medium、long 分别约为 2.55、7.67、9.29，
说明当前 treatment 或评分定义尚不能形成可靠的能力测量。实现、验证和分析
代码暂时保留，可通过 `--capabilities` 显式启用，便于后续重新设计和专项实验。
层级一致性仍为 qualification-only，因此默认正式生成与排名由其余八个能力
组成。既有实验产物保持不变，后续默认实验使用新的 experiment id。

## 2026-08-23 — Medium/long 使用当前数据与官方配置的交集

CaFE 当前二十个 GIFT-Eval 配置中，medium 和 long 只运行与官方固定
medium/long 清单相交的十一个配置，不再把所有 short 配置机械切换 term。
TiRex2 的最大输出长度为 320，低于 medium/long 的 480–900 点 horizon，
因此只参加 short；medium/long 固定使用其余六个模型。Generation 的真实
模型上下文距离门、validation 和 inference 均使用同一 term-specific 模型
集合。推理在加载模型前核对输出及 future-covariate 长度，并把输出 horizon
成本计入 bulk 大小与 endpoint 动态并发。该协议使用 CaFE v14 和新的实验 id。

## 2026-08-22 — 多机推理采用可选的近端请求重建

多台推理服务器同时可用时，timecho92 继续负责总调度和最终 manifest，
但每个 endpoint host 按稳定 source shard 分区在本机重放 compact contract、
组装 MessagePack bulk、请求本机服务并写预测分片。总调度回收分片后核验
文件哈希和分区覆盖。这样同时使用三台机器各自的 CPU、内存带宽和 loopback，
避免由 timecho92 的单一数组重建流水线限制十二张 GPU。

该模式由逐 endpoint 的 `--distributed-worker` 参数显式启用，可配置一、二或
三台；不提供该参数时保留原单机供给路径。每次实验的 worker 拓扑写入 stage
contract，中途不改变。各 worker 使用模型最大 context 感知的 suffix replay，
完整历史仍决定已冻结 treatment，只省略模型最终不会接收的前缀物化。

## 2026-08-22 — 正式样本要求预测区间标签完整

CaFE 使用 GIFT-Eval 固定测试起点，但不沿用其对未来 NaN 的逐点评分
mask。一个候选实例的完整预测区间 \(H\times D\) 中只要存在任一缺失
target，该实例就不进入 generation、inference 或 analysis。部分缺失和
全部缺失分别计数并写入 generation manifest；多变量实例要求所有目标
通道的未来标签均完整。这样保证原始 MASE、treatment MASE 与机制响应
使用一致且完整的真实评分标签，也避免为最终不会计分的窗口执行模型推理。

该规则只约束预测区间标签。历史缺失仍作为真实输入质量问题，使用仅依赖
历史的确定性插值处理，不读取未来数据。本次样本集合变化启用新的 v12
pipeline 和 experiment id，不改写 v11 实验产物。

## 2026-08-22 — 多季节性等级改为周期数量并提供人工 anchor

多季节性不再把五级解释为“逐级加强第二频率”。Level 1 至 Level 5
现在分别包含 2、3、4、5、6 个受控周期；每个周期在聚合前具有相同的
完整历史能量，五档聚合后再分别归一化到同一个、按实例共享的 macro
RMS，范围为 `[0.22,0.28]`。因此等级只增加模型需要同时分解和外推的
周期数量，不同时增加 treatment 的总幅度。

每个目标通道先在真实历史去趋势功率谱的前三个候选中依次寻找 anchor。
候选必须落在模型可见的周期范围内，完整历史和前后两半都达到 0.05
源尺度，并保持足够一致的相位和振幅。前三个都不合格时，不继续从更弱
谱峰中挑选，而是从协议的 512 点对数周期池确定性生成一个人工 anchor；
其余五个附加周期也从同一池中生成。任意周期对都必须具有足够频率分离，
并避开 2 至 8 倍整数谐波附近 5% 的范围。真实曲线始终是 treatment 底座，
“人工”只指叠加的受控周期分量，所有周期都以解析形式连续延伸到未来，
参数选择不读取官方未来目标值。

在本地 20 个 GIFT-Eval short 数据配置、120,784 个预测标签完整的官方实例
审计中，最终多季节性 treatment 可用率为 100%；按目标通道统计，57.18%
使用前三个候选中的真实 anchor，42.82% 使用人工 anchor。前三候选上限是
在可解释性、真实 anchor 覆盖率和实现稳定性之间的固定协议选择。该变更启用 CaFE v13
及新的 generation、contract、validation、inference、analysis schema，
既有已完成实验保持不可变，重跑使用新的 experiment id。

## 2026-08-21 — 非线性能力改为状态依赖持续性

原“居中状态平方加性分量”不再作为正式 nonlinear persistence
treatment。它的非线性项不反馈进后续状态，处理后历史也不严格
服从所声称的递推律；且线性状态回到均值时，居中平方分量
可趋向常数而不是归零。

新机制使用符号保留的有界二阶响应
`g(z)=z|z|/(1+|z|)`，使状态幅度直接改变有效 AR(1)
持续系数。Treatment 历史使用真实历史的同一组线性创新递归生成。
预测期只从历史线性创新中构造 128 条居中循环分块重采样路径，
每条路径同时驱动线性与非线性状态，再对路径内差值取均值，作为
正式 future effect。这样评测的是共享未知冲击下的条件均值响应，
不要求 zero-shot 模型复现通常不等于非线性条件均值的确定性骨架。

Generation 必须先做一次 blocked suffix holdout：非线性结构基线
在留出历史上的一步 MSE 必须优于线性 AR，并在最多四个历史伪起点的
创新边际化多步预测中继续取得正收益；同时满足普通/极端
状态支持、前后半系数方向稳定、动力学稳定、五级距离递增和
预测期效应可见性。Research validation 核验存储证据，publication
validation 从 Arrow 重新生成并精确比对。Inference 不执行科学门控。
Analysis 保留 pooled effect NRMSE 为排名主分，新增峰值归一化
衰减形状 NRMSE 和半衰期误差作为诊断，预测段内未减半显式记为
censored。五级直接控制“已检出方向上使用的稳定性余量比例”，
历史 RMS 仅作通用可见性和安全门。本轮不引入 matched linear
control。变更使用新的 generation/contract/validation/analysis schema，
不改写已完成实验。

## 2026-08-21 — 协变量能力遵循源 benchmark 的可见性语义

协变量能力改为使用源 benchmark 的原生协变量字段，不再用由
frequency 和索引生成的人工 calendar path 代替它们。适配器必须保留
源字段的信息边界：past-only 协变量只进入历史请求，known-future
协变量只在源 benchmark 明确宣告预测期可见时进入未来请求；
没有原生协变量的数据集将该能力记为 unavailable，不合成替代特征。

更新后的能力名称为“协变量冲击响应”（Covariate Impulse Response），
capability id 为 `covariate_impulse_response`。在原生协变量通道上加入
有界、可重放的冲击，并通过固定因果核在目标上构造延迟响应。历史中
放置多组“协变量冲击 → 目标响应”示例，并在 forecast origin 之前
放置末端冲击。past-only 任务的预测期 effect 只是这个已观测末端
冲击的响应尾部，不向模型提供或要求其猜测未来协变量；
known-future 任务才可在预测期继续提供协变量冲击路径。

因果核的末端冲击幅度由历史尺度和 observed mask 归一化，保证五档
future effect 的 MASE 标准化 RMS 均不低于 0.05。机制可评分 gate
只作实现不变式校验，不再通过删除低能量样本弥补自由衰减的设计
缺陷。正式归因保留输入消融：保持
treatment target 不变，只破坏协变量冲击与目标响应的时间对齐，
避免将目标自身的可外推性误报为协变量利用能力。参数与冲击位置
的选择只使用 forecast origin 之前的信息。该变更使用新的
experiment id 和 stage contracts，不改写已完成实验。

## 2026-08-17 — 实验产物采用契约重放和 Parquet

GIFT-Eval Arrow 文件作为真实序列的唯一副本。Generation 仅保存官方样本
索引、treatment/ablation 重放契约、门限和哈希，不重复保存完整 target、
future 或 calendar covariates。契约、预测和逐样本指标使用 ZSTD Parquet，
manifest 与小型汇总保留 JSON；模型请求在内存中通过 MessagePack bulk
发送，不生成模型专属 task 文件。这样兼顾批量读写、恢复、并行扫描和
数十 GB 级全实验存储预算。

## 2026-08-17 — 生成、推理和分析采用有界并发

Generation 按稳定 source shard 使用多进程拟合契约；推理按模型将同一
模型加载到所有兼容 endpoint/GPU，并以有界 shape buffer 并发发送 bulk
请求；预测按 source shard 原子写入，analysis 逐 shard 重放和聚合。并发
队列同时受 batch 数和 shard 边界约束，避免用全量内存换取吞吐。

## 2026-08-17 — 准确性、机制响应与结构归因分别报告

分析同时报告原始样本和 treatment 的 MASE/MAE；机制分继续使用配对
effect NRMSE。common factor 与 cross-series dependence 另设输入消融表，
保持被评估目标历史和 treatment future 不变，只错位辅助通道历史，并以
消融前后的 MASE 变化衡量模型是否有效利用跨序列输入。三类结果不合成
单一总分，避免将预测准确性、反事实响应和结构输入归因混为一谈。

## 2026-08-17 — Treatment 与原样本的距离只设下限

Treatment-to-source 距离校验只用于防止增强样本与原样本过于接近，
不再设置最大距离或最大单通道距离。原因是项目需要的是最小分离保证，
距离上限会额外删除强增强样本，不属于当前 benchmark extension 的目标。

## 2026-08-17 — 研究阶段默认只执行距离门验证

日常研究流水线的 validation 并行扫描全部 treatment contract，只确认每条
treatment 通过相对官方原曲线的最小距离下限，并生成 inference 所需的
绑定报告。Manifest、文件与原始 Arrow 哈希校验、完整官方样本重遍历、
全部 treatment 确定性重放及逐字段比较保留为显式 publication 模式，供
发布冻结实验使用。这样保留研究阶段关心的污染防护门，同时避免在每轮
实验中重复完整 generation 计算。

## 2026-08-18 — 正式推理使用真实批处理与 FP32+TF32

推理服务的 bulk 请求保留为一次模型级 batch forward，CaFE 按输入
元素数同时限制单请求和 endpoint 在途成本。Toto 与 FlowState 中原有
的逐 item GPU forward 改为真实 batch；其他 GPU 模型继续使用已有的
native batch 路径。Toto 正式结果使用 FP32 参数和 TF32 矩阵核；BF16
虽有更高吞吐，但对真实 GIFT 样本的预测已出现可测差异，因此只作
显式吞吐模式，不作为默认研究精度。

## 2026-08-18 — Replica数量按模型实测配置

四卡推理不采用统一replica数量。short-term正式配置中，Timer-4.0、
Moirai2、TimesFM2.5和TiRex2每卡加载2个replica；Chronos-2、Timer-3.5
和Toto2.0每卡保留1个。选择来自同一服务端、相同bulk与token预算下的逐模型
扫描：部分轻量模型可用双replica提高GPU占用，Chronos与Toto双replica反而
降低吞吐，Timer-3.5双replica会显存不足。CaFE按模型依次加载并跨数据集复用
拓扑；medium/long任务可按独立测量结果覆盖short-term默认值。

## 2026-08-19 — 完整历史定义强度，真实模型上下文执行距离上下限

本决策替代 2026-08-17 的“Treatment 与原样本的距离只设下限”。固定的
96、168、336、512、1024 后缀不对应当前七个参评模型的实际最大输入，予以
删除。幅度型能力只用完整官方历史的 macro 归一化 RMS 定义五档强度并计算
物理增益；距离资格检查使用 TiRex2 2048、Timer-4.0/Chronos-2 8192、
Timer-3.5 11520、TimesFM2.5 15360、Moirai2/Toto2.0 16384 经当前实例
历史长度截断和去重后的真实模型上下文。每个上下文要求 macro 距离不低于
0.10、不高于 2.0，任一受影响单通道距离不高于 3.0。检查结果不再反向放大
分量；任一级越界时整个实例能力组不可用，并在后续实验中按能力和数据集
审计越界原因。

## 2026-08-21 — 机制分要求可见未来效应并使用 pooled NRMSE

Treatment MASE 对所有有效未来继续计算。机制响应只在真实 future treatment
effect 经官方历史 MASE scale 标准化后的 RMS 不低于 0.05 时计算；低于门限
记为 `unavailable_low_truth_effect`，不再用极小 epsilon 强行充当分母。正式
机制分使用所有可评分单元的标准化平方误差和真实效应平方和之比开方，即
pooled NRMSE；同时报告有效覆盖率、低信号数量以及逐样本诊断统计。

Predictable intermittency 的五档仍只控制事件稀疏度并共享一个脉冲幅度。
相位从确定性候选中选择，使预测区间内至少一个事件落在官方 observed mask
的有效位置；共享幅度同时满足历史 treatment 距离与五档未来效应可见性。
该过程只使用历史尺度、预测长度和 observed mask，不使用未来目标值或模型
输出。这样避免稀疏档在评分窗口没有有效事件，却产生零分或极大 NRMSE。

## 2026-08-21 — Horizon support 按能力机制分别检查

不采用所有能力统一的固定长度或三等分门。TVS 从真实历史残差拟合受约束
AM 的包络频率、幅度与相位，检查整个预测段的包络有效覆盖；common factor
保留真实 PCA loading，但将会指数衰减的 AR(1) future rollout 改为从真实
latent factor 拟合的恒振幅谐波，只对 common 检查相对首尾三分段的能量比。
Trend、regime 和解析季节分量由构造保证；intermittency检查预测段存在有效
事件；nonlinear persistence允许自然衰减并单独报告衰减行为。

Generation 负责拟合、资格判定和失败时不生成 treatment。默认 research
validation 并行检查 compact contract 中的距离、机制可评分性与适用的能力
horizon-support gate；publication validation 再从原始 Arrow 完整重放并精确
核验。这一分层既防止不合格样本进入推理，也避免研究阶段重复 dense replay。
## 2026-08-22：多机推理保持可选，生成分片与当前机器数解耦

- 决策：近推理端 replay 通过显式 endpoint→worker 参数启用；不传参数时继续使用单机调度。生成文件不绑定当前机器数，大数据集保持紧凑分片，小数据集规划约三个 source shards。
- 原因：实验期间可使用三台四卡服务器提高吞吐，但后续可能只剩一台；同一架构需要同时兼顾单机可运行、三机负载均衡和 Parquet 文件数量。

## 2026-08-26 — 八项默认能力采用跨 level 共享的结构随机性

固定官方实例集合和预测起点，不引入样本选择随机性。`augmentation_seed`
改为先为每个“官方实例 × capability”选择一个结构，该结构由五个 level
共享；level 继续只改变原有的 RMS、周期数、变点位置或事件间距坐标。Trend、
multi-seasonal、time-varying seasonality、regime switching、predictable
intermittency、common factor、cross-series dependence 和 covariate impulse
response 均增加结构 family、通道/边/载波或响应核等 seed 抽样。

Trend 的分段线性规律覆盖完整 treatment history 与 forecast horizon，预测期
允许按相同 knot interval、phase 和 slope cycle 出现新分段点，不增加“预测期
必须单调”之类的额外资格门。继续使用现有历史资格、source-distance 以及必要
的未来可评分或 horizon-support 检查。结构参数、结构哈希和跨 level 共享状态
写入 compact contract，并用新的 v15 pipeline 和实验 id，避免改写已完成实验。

## 2026-08-27 — 候选相关资格门先于 augmentation seed 抽样

官方实例集合以及 capability availability 不随 augmentation seed 改变。对
cross-series dependence，复用已有预测边排序，只在最多前 48 条边上计算现有
source-distance 的可行 dose，并保留最多 12 条合格边；对 covariate impulse，按
实例固定构造最多 8 个与 augmentation seed 无关的结构描述，先检查现有
source-distance 上下限和最低 future-effect，再由 seed 从合格池抽样。

不枚举 covariate、target、kernel 等参数的完整笛卡尔积，不为候选生成五个
level，也不增加新的资格门。该顺序修复使用新的 mechanism v12 和 structural
randomness v2 标识；此前两批 v15 generation 产物保留为诊断数据，不改写。

候选不再要求整个 nominal level 区间都通过门限。对每个结构解析计算
source-distance（以及需要时的 future-effect）允许的坐标范围，各 level 从其
nominal 区间与该范围的交集均匀抽样；只有某个 level 的交集为空时才淘汰结构。
五档 nominal RMS 区间调整为 `0.10–0.15`、`0.17–0.22`、`0.25–0.32`、
`0.36–0.46`、`0.50–0.65`，保留 Level 1 下界并适度增强高 level。

## 2026-08-29 — Moirai2 单副本并收紧可复现运行上下文

四卡 short-term 吞吐扫描中 Moirai2 每卡两副本更快，但该配置只在较短
历史上成立。FEV Mini 的 `boomlet_1676` 在独立单变量适配后使用约
8.5k–10.4k 历史；Moirai2 的单请求注意力偏置还需额外 7–8.3 GiB，
两副本在 32 GiB GPU 上相互挤占并产生 OOM。

单副本实测进一步确认，L8543–L8831 的 184 个视图成功，但从 L8927
开始仍会 OOM。因此 Moirai2 默认改为每 GPU 一副本，并将 CaFE 的
可复现运行上下文上限从服务宣称的 16384 收紧为 8192。生成距离资格门
和推理截断都绑定这一上限；服务可以宣称更大上限，但不得低于实验协议。
这是 32 GiB 实验硬件上的正确性上限，而不是 FEV 专用特例；代价是短历史
上 Moirai2 吞吐降低。失败实验保留，新协议使用新实验 id 从头运行。

## 2026-08-30 — Moirai2按实际child row限批并恢复16k上下文

本决策替代 2026-08-29 中将Moirai2上下文永久收紧为8192并改为单副本的
部分。复查发现原有`maximum_bulk_rows=8`约束的是拆分前parent
sample；FEV的100维panel经独立单变量适配后，一个parent仍会在同一服务请求
中展开为100个child row。该请求在H=96的分位数自回归中进一步放大activation，
所以先前L8927附近的OOM不是batch=1上下文上限。

隔离测试中，32 GiB RTX 5090上的C=16384、B=1、H=96峰值allocated为
0.85 GiB；B=8、H=96为6.46 GiB；B=8、H=900为7.24 GiB，均成功。CaFE因此
在传输边界按实际child row切分bulk，保持parent级结果顺序和重组语义不变，
并将Moirai2生成距离契约及推理上下文恢复为16384。双副本压力测试在单张卡上
并发4个C=16384、B=8、H=900请求，全部成功，两worker各驻留约9.1 GiB，
总显存峰值18.1 GiB。因此默认部署恢复为每GPU两个副本；实际child 8行上限
作为32 GiB硬件安全边界。若服务端上限低于16384，仍以服务实际声明为准。
